<div class="yui-g">
  <% form_for :membership, @membership, :url => user_group_memberships_path(@user_group) do |f| %>
    <%= render :partial => 'form', :locals => { :f => f} %>
    <br/>
    <%= submit_tag 'Join' %>
  <% end %>
</div>
